name: Auto Update Congress Profiles Debug

on:
  workflow_dispatch:

jobs:
  update-profiles:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Show directory tree
        run: |
          echo "GitHub working dir:"
          pwd
          echo "Files:"
          ls -R
