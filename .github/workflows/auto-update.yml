name: Debug Test Message

on:
  workflow_dispatch:

jobs:
  debug-test:
    runs-on: ubuntu-latest

    steps:
      - name: Print unique test line
        run: echo "🎯 This is the ONLY test workflow in this repo 🎯"
